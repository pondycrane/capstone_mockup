/**
 * Display user credentials.
 */

import React from 'react';
import PropTypes from 'prop-types';

export class Issuer extends React.PureComponent {
  render() {
    return (
      <div>
        <p>This is issuer area</p>
      </div>
    );
  }
}

export default Issuer;
