/**
 * Display user credentials.
 */

import React from 'react';
import PropTypes from 'prop-types';

export class Recepient extends React.PureComponent {
  render() {
    return (
      <div>
        <p>This is recepient area</p>
      </div>
    );
  }
}

export default Recepient;
