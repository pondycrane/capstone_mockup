/**
 * Display user credentials.
 */

import React from 'react';
import PropTypes from 'prop-types';

export class Profile extends React.PureComponent {
  render() {
    return (
      <div>
        <p>This is profile area</p>
      </div>
    );
  }
}

export default Profile;
